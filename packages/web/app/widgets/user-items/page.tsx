/**
 * User Items Widget Page
 *
 * TEMPLATE: This is the page wrapper for the widget component
 * Keep this simple - just import and render your component
 */

import UserItemsWidget from "./widget";

export default function UserItemsPage() {
  return <UserItemsWidget />;
}
